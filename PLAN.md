# Scratchy (스피또 스타일 모바일 스크래치 카드) 개발 계획서

본 프로젝트는 모바일 환경에 최적화된 바닐라 자바스크립트 기반의 가상 스크래치 카드 서비스입니다. 사용자는 복권을 긁는 듯한 시각적/촉각적 피드백을 경험하고, 자신만의 메시지를 담아 링크로 공유할 수 있습니다.

---

## 1. 프로젝트 개요

- **목표**: 오프라인 복권(스피또)의 "긁는 재미"를 웹 기술로 모바일 화면에 구현.
- **핵심 경험**:
  - 부드러운 스크래치 동작과 시각적 드러남.
  - 긁을 때마다 발생하는 미세 햅틱 진동 및 티켓 흔들림.
  - 자신만의 메시지를 16자 이내로 입력하여 링크 공유.
- **제약 조건**: Serverless (모든 데이터를 URL 파라미터/해시에 보관).

---

## 2. 기술 스택

- **코어**: Vanilla JavaScript (ES6+), HTML5 Canvas, CSS3
- **빌드 및 개발 도구**: Vite (최신 웹 표준 번들러, 핫 리로딩 및 모바일 테스팅 최적화), Bun 사용
- **배포/이용 환경**: 모바일 브라우저 (iOS Safari, Android Chrome 등) 타겟

---

## 3. 아키텍처 및 폴더 구조

바닐라 JS의 유지보수성을 극대화하기 위해 관심사에 따라 모듈화된 설계를 적용합니다.

```
/scratchy
├── index.html            # 메인 앱 진입점 및 모바일 뷰포트 레이아웃
├── package.json          # Vite 및 개발 종속성
├── vite.config.js        # 빌드 및 로컬 서버 설정 (모바일 접속용 호스트 개방)
├── src/
│   ├── main.js           # 앱 초기화 및 상태 관리 (State Machine)
│   ├── style.css         # 테마 스타일, 폰트 및 프리미엄 CSS 이펙트 (Shake, Glow)
│   ├── components/
│   │   ├── scratch-card.js # Canvas 드로잉 및 터치 트래킹, 긁개 제어
│   │   ├── haptic.js      # Vibration API 관리 및 iOS 폴백 처리
│   │   └── sharer.js      # URL 파라미터 파싱 및 공유 링크 생성 (Base64)
│   └── utils/
│       └── confetti.js    # 복권 긁기 완료 시 축하 효과 (Canvas Confetti 직접 구현)
└── PLAN.md               # 개발 계획서 (본 파일)
```

---

## 4. 핵심 기능 구현 상세 계획

### A. HTML5 Canvas 기반 스크래치 메커니즘 (`scratch-card.js`)

1. **은박 커버 레이어 드로잉**:
   - Canvas에 은색 그라디언트(Metallic Silver Gradient)와 복권 특유의 노이즈 패턴 또는 "Scratch me!" 텍스트를 드로잉하여 오프라인 느낌 재현.
2. **긁기 동작 구현**:
   - `touchstart`, `touchmove`, `touchend` 이벤트를 감지하여 터치 궤적 계산.
   - `globalCompositeOperation = 'destination-out'` 속성을 사용하여 궤적을 투명하게 지움으로써 아래의 숨겨진 메시지 노출.
   - 터치 감도와 긁는 브러시 크기(Radius 20-30px) 최적화로 모바일에서 시원시원하게 긁히도록 구현.
3. **진척도(Clear Rate) 계산**:
   - 성능 저하를 방지하기 위해 `touchmove` 발생 시 100ms 디바운싱(또는 긁는 중 주기적 샘플링)을 적용하여 Canvas 픽셀 데이터를 분석.
   - 전체 픽셀 대비 투명해진 픽셀의 비율이 **75%** 이상에 도달하면 카드를 부드럽게 Fade Out 시켜 전체 메시지 완전 노출.

### B. 햅틱 진동 및 티켓 흔들림 효과 (`haptic.js`, CSS)

1. **모바일 햅틱 진동**:
   - 터치 좌표가 이동하는 순간(스크래칭 진행 중) `navigator.vibrate(10)`을 짧게 호출하여 사각거리는 마찰감 표현.
   - **iOS Safari 지원 불가 대응**: iOS Safari는 Vibration API를 지원하지 않으므로, 에러를 방지하는 예외 처리를 적용하고 시각적/청각적(원할 경우 효과음) 피드백으로 우회.
2. **티켓 흔들림 (Physical Interaction)**:
   - 긁는 터치 이동 속도(velocity)와 방향에 따라 티켓 엘리먼트에 미세한 2D transform (`translate`, `rotate`) 및 쉐이크 애니메이션을 동적으로 인가.
   - 긁는 강도가 강할수록 흔들림의 진폭이 미세하게 커지는 다이내믹 인터랙션 설계.

### C. 서버리스 메시지 커스텀 및 URL 공유 (`sharer.js`)

1. **메시지 규칙**:
   - 영문/한글/공백 포함 최대 16글자 제한.
   - 유효성 검사 및 바이트 수 제한 체크 UI 제공.
2. **URL 데이터 인코딩**:
   - 사용자가 작성한 커스텀 메시지를 `UTF-8` 바이트 배열로 변환한 뒤, URL 세이프한 `Base64` 문자열로 변환.
   - 예: `https://.../?msg=SGVsbG8gV29ybGQ=` 형태로 URL 구성.
3. **공유 모드 vs 긁기 모드**:
   - **메시지가 없는 일반 진입**: "나만의 스크래치 카드 만들기" 작성 폼 화면 노출.
   - **URL에 `msg`가 포함된 진입**: 작성 폼은 숨겨지고, 전달된 메시지가 스크래치 카드 하단에 배치된 상태에서 즉시 스크래치 플레이 모드로 시작.

### D. 프리미엄 UI/UX 디자인 가이드라인

- **색상 테마**: 딥 네이비/차콜 베이스의 다크 테마에 네온 퍼플/핑크 그라디언트를 가미하여 트렌디하고 세련된 느낌을 제공.
- **글꼴**: 모던 웹 폰트(Google Fonts - 'Inter' 및 'Outfit') 사용.
- **글라스모피즘(Glassmorphism)**: 공유 링크 생성 창 등 모달 UI에 `backdrop-filter: blur()`와 미세한 반투명 보더 적용.
- **동적 연출**: 긁기 완료 시 Canvas Confetti(폭죽) 이펙트가 화면 전체에 뿌려지며 카드 뒤쪽에서 빛이 새어 나오는(Glow Expansion) 애니메이션 적용.

---

## 5. 단계별 마일스톤 및 개발 계획

1. **Phase 1: 프로젝트 기반 설정**
   - Vite 환경 구성 및 CSS 스타일 시트 기본 아키텍처 수립.
   - 모바일 최적화 뷰포트 설정 (`user-scalable=no`).

2. **Phase 2: 핵심 스크래치 & 피드백 인터랙션 구현**
   - Canvas 스크래치 기능 개발.
   - 터치 이벤트 기반 Vibration 연동 및 CSS 쉐이크 이펙트 구현.

3. **Phase 3: 메시지 편집 및 URL 공유 기능 개발**
   - Base64 인코딩/디코딩 및 URL 파라미터 로딩 엔진 구현.
   - 16자 제한 텍스트 인풋 폼 및 링크 복사 기능 개발.

4. **Phase 4: 디자인 디테일링 및 예외 처리**
   - 프리미엄 CSS 이펙트(은박 메탈릭 텍스처, 축하 애니메이션) 적용.
   - 다양한 모바일 기기(iOS/Android) 교차 테스트 및 폴백 기능 안정화.
